---
title: "REPLACE — a real sentence, Title Case"
dek: "REPLACE — one sentence promising what the reader will take away."
category: "REPLACE — one of the established categories"
date: "REPLACE-YYYY-MM-DD"
accent: "#REPLACE"   # warm/earthy, matching the category — see post-schema.md
glyph: "REPLACE"
repo: "REPLACE-source-repo-name"
stack: ["REPLACE", "REPLACE"]
readingTime: "REPLACE min read"
featured: false
order: REPLACE
tags:
  - REPLACE
  - REPLACE
---

<p class="lead">REPLACE — open on a vivid, specific tension. This is a quiet, slightly-larger intro paragraph (no drop cap, no ornament), so make the words carry it. State the problem or the surprise that made this experiment worth doing. No throat-clearing.</p>

Set up the stakes in a sentence or two of plain prose. What did you want, and why was it harder than it looked?

## REPLACE — first move

Explain the first real step in the thinking. Show the concrete decision, not the abstraction.

```text
REPLACE — a tiny diagram or API sketch, only if it clarifies
input ─▶ [step] ─▶ output
```

<div class="callout">
<span class="label">The core idea</span>
REPLACE — the single line you want the reader to remember. Make approval expensive; make the principle quotable.
</div>

## REPLACE — second move

Develop the idea. Keep it concrete and honest.

<!-- OPTIONAL VISUAL — prefer an inline SVG diagram or a scrollytelling block over a
     raster image. Copy a working example from posts/05-from-audio-to-notation.md and
     see reference/media.md. Remove this block if the essay is text-only. -->
<div class="figure-block draw">
<svg class="diagram" viewBox="0 0 640 140" role="img" aria-label="REPLACE — what it shows">
<rect x="8" y="36" width="160" height="56" rx="12" class="d-stroke d-soft"/>
<text class="d-label" x="88" y="62" text-anchor="middle">REPLACE</text>
<line data-draw pathLength="1" x1="172" y1="64" x2="224" y2="64" class="d-stroke d-accent"/>
</svg>
<div class="caption">REPLACE — one line on what the reader is looking at.</div>
</div>

## What broke, and what it taught me

- **REPLACE failure mode.** What happened, and the lesson it forced.
- **REPLACE failure mode.** What happened, and the lesson it forced.

## REPLACE — the reusable pattern

Zoom out. Nothing here is really about <em>this specific project</em> — name the
transferable principle and tell the reader exactly what to steal for their own work.
